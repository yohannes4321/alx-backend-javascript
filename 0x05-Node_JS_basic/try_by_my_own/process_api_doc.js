// Process
//process object global object accessed everywhere

const { CommandStartedEvent } = require("mongodb")

/** 
 * -0 status code when there is no async operations are pending
 * uncaught fatal exception not hadled by domain 
 * internal js parse error error during developing 
 * process.exit(1)
 * process.on('exit',(code)=>{
 * console.log('about with exit code ',code )})
 * 
*/
 
  
 
